FROM node:18-alpine as builder

WORKDIR /app

COPY . .

RUN \
	npm i -g pkg@^5.8.1 && \
	npm ci && \
	pkg index.js --output handler

FROM alpine:latest

WORKDIR /

COPY --from=builder /app/handler .

CMD ["./handler"]
